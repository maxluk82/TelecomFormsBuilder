{
  "name": "cmhk-secure-tool",
  "private": true,
  "version": "1.0.1",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build": "node build.cjs"
  }
}
